# Random VTuber Toys

A collection of fun tools for VTube Studio models.

## Toys

### [GetDown](GetDown/)

Controls your VTuber model through totally random number generation

Available as a **standalone Python script** or a **Streamer.bot extension**.

## VTube Studio Setup

Both tools need VTube Studio's API turned on. Here's how:

1. Open **VTube Studio**
2. Click the **gear icon** to open Settings
3. Scroll down to **General Settings**
4. Find **"Start API"** and turn it **on**
5. Note the **port number** shown (default is `8001`) — you'll need this later
6. When you run one of the toys for the first time, VTube Studio will show a popup asking you to **allow the plugin** — click **Allow**
